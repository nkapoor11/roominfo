<p id="notice"><%= notice %></p>

<h1>Rooms</h1>

<h5>Add your rooms if you are a faculty. Students may view a room (show) but only faculty can create, edit, or destroy a room.</h5>

<table>
  <thead>
    <tr>
      <th>Location</th>
      <th>Number</th>
      <th>Room type</th>
      <th>Description</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @rooms.each do |room| %>
      <tr>
        <td><%= room.location %></td>
        <td><%= room.number %></td>
        <td><%= room.room_type %></td>
        <td><%= room.description %></td>
        <td><%= link_to 'Show', room %></td>
        <td><%= link_to 'Edit', edit_room_path(room) %></td>
        <td><%= link_to 'Destroy', room, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Room', new_room_path %>
